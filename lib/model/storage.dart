import 'package:flutter/material.dart';

class Storage{
  late String storageName;
  late String storageSize;
  late Color storageColor;
  late int currentStep;

  Storage(this.storageName, this.storageSize, this.storageColor, this.currentStep);
}